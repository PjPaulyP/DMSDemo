# Next Steps

- Review organizing new subfolders under each plant. Consider making it `Units`, `Documents`, and `Turnarounds`. Ensure data stays connected and avoid file duplication.
- Create a new page where the top half is the `Controlled` document process and the bottom half is the `Uncontrolled` process. Link the `Controlled`, `Uncontrolled`, and `Request Changes` actions to this page.
- Set up user roles and profiles. Profiles should indicate the main plant, and roles should control which folders a user can edit.
- Add hover details for `Approved` status to show the approval process ID and approval process initiator.
- Consider adding a document preview feature that also shows revision history when clicking a file.
- Add an AI button feature.
